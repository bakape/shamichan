* Update `syncs` in client when on /live
* Actually tell user when a particular thread failed to sync
* Extract common fields (num, op, type) from log (since these are duplicated in
  publications for filtering)
* Tokenize text instead of multiple levels of regexps
* Look for memory leaks in the front page
  * Also redis connection count
* When navigating away from a page, commit any outstanding text
* Backbone.js
* Hover previews of threads
* Inline expansion messes with prediction?
* Inline expansion should remake the img
* YouTube expansions should be closeable
* YouTube links should have a title
* Don't use keys() for loading OPs
* Incoming JSON schema checker
* Upload cancelling doesn't work?
* Cross-board links should be direct
